[package]
name = "micro_os"
version = "0.1.0"
edition = "2024"

#Don't need these any more since we specify
#abort in .json file
#[profile.dev]
#panic = "abort"
#[profile.release]
#panic = "abort"

[[bin]]
name="micro_os"
test = false
bench = false

[dependencies]
bootloader = "0.9"